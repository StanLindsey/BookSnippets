{
  "name": "slappedUp App",
  "version": "1.0.0",
  "description": "Just an app we slapped together one afternoon.",
  "scripts": {
    "start": "meteor --settings settings_dev.json",
    "staging": "meteor deploy staging.meteor.com --settings settings_dev.json",
    "production": "meteor deploy production.meteor.com --settings settings_prod.json"
  }
}
